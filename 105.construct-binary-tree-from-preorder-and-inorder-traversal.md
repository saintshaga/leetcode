```
用递归的思路

首先在preorder中拿到元素，然后到inoder中把序列分成两边，左边递归调用得到的node赋给left，右边递归调用得到node赋给右边

困难的地方是：左子树的根是i+1，那么右子树的根在哪儿呢？
我采用的方法是把preoder元素放在队列里面，然后一个个消费
事实上，左子树的个数已经能算出来了，只要把preoder中当前元素i+n(左子树的个数)+1就是右子树的根了
```
